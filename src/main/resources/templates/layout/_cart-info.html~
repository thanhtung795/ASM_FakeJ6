<div class="card mt-3 mb-3" style="width: 18rem;">
  <div class="card-header">
    <i class="bi bi-cart-fill me-2"></i> Shopping Cart
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <a href="/cart/view" class="btn btn-outline-primary">View Cart</a>
      <span class="badge bg-success rounded-pill d-flex flex-column align-items-center">
        <span class="fs-6">{{(cart.count?cart.count:0)}} items</span>
        <span class="fw-bold">${{(cart.amount?cart.amount:0)}}</span>
      </span>
    </li>
  </ul>
</div>
